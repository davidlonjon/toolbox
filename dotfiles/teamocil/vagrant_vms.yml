session:
  name: "vagrant_vms"
  windows:
    - name: "dev"
      root: "/Users/david/projects/dev"
      options:
        synchronize-panes: false
      panes:
        - cmd: "vagrant status"
    - name: "soompi_dev"
      root: "/Users/david/projects/soompi_dev"
      options:
        synchronize-panes: false
      panes:
        - cmd: "vagrant status"