## -------------------------------------------------------------------
## ALIASES SPECIFIC TO SOME PROJECT
## -------------------------------------------------------------------

### -------------------------------------------------------------------
## SPP PROJECT ALIASES
### -------------------------------------------------------------------
alias xwp-reset-dev="checkout-develop-branches.sh && quick-clone-repos.sh"
alias xwp-checkout-dev="checkout-develop-branches.sh"
alias xwp-delete-merged-branch-into-develop="git checkout develop --quiet && git branch --merged | grep -v 'master' | grep -v 'develop' | xargs -n 1 git branch -d"
alias xwp-list-merged-branch-into-develop="git checkout develop --quiet && git branch --merged | grep -v 'master' | grep -v 'develop'"